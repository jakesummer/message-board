# Database connection string for PostgreSQL
DATABASE_URL="postgresql://<username>:<password>@localhost:5432/<db_name>"

# Environment mode: either "dev" or "prod"
NODE_ENV="dev"

# Optional: port for server to run on (defaults to 3000)
PORT=3000